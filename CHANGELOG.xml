<?xml version="1.0" encoding="utf-8"?>
<extensionset>
    <extension name="Facility Calendar Upcoming Event List — Modern Soft Blue" packagename="facilitycalendar-upcomingeventlist-modernsoftblue">
        <notes>
            <note version="1.4.2" creationdate="2026-09-01">Fixed extension display names showing raw language keys (PKG_FACILITYCALENDAR_UPCOMINGEVENTLIST_MODERNSOFTBLUE and MOD_FACILITYCALENDAR_UPCOMINGEVENTLIST_MODERNSOFTBLUE) in the Joomla admin Extensions list. Joomla 3 resolves a package name via .sys.ini and a file extension's name via a files_&lt;element&gt;.sys file; the file manifest's MOD_... key is not resolvable for a file extension, so the raw key was shown. Replaced the package and file manifest &lt;name&gt;/&lt;description&gt; with literal friendly strings (JText::_ returns literals unchanged), matching the update.xml name, so the real name always displays regardless of language-file loading. Installer-localized (PKG_...) error messages are unaffected.</note>
            <note version="1.4.1" creationdate="2026-09-01">Fixed the Joomla 3.10 install abort. The v1.4.0 (and earlier) package failed to install because the file manifest declared &lt;languages folder="language"&gt; while the files actually live under files/language/, so the language files could not be found and the installer aborted mid-install. The package manifest also listed CHANGELOG.xml as a child extension and referenced inert &lt;install&gt;/&lt;update&gt;/&lt;media&gt; blocks that the Joomla 3 file adapter ignores, so modernsoftblue.css was never deployed. This release points both &lt;languages&gt; declarations at the real folders, removes CHANGELOG.xml from the package files list, ships modernsoftblue.css via the Joomla-3 fileset to media/mod_facilitycalendar_upcomingeventlist_modernsoftblue/, and drops the inert blocks. Also repointed the package's own pkg language folder so the installer script's messages localize.</note>
            <note version="1.4.0" creationdate="2026-09-01">Update-server hardening and installer cleanup: added a SHA-256 checksum to update.xml so the Joomla Update component (and Joomla 4, where a mismatch is a hard failure) can verify the downloaded package integrity. Replaced leftover inline installer error strings with the existing language keys so all user-facing messages are localizable. Reworded the badge-missing message to match the new behaviour (the badge is now shipped by the package). Removed the extraneous patch/mod_facilitycalendar_event_list.xml, which was a throwaway target manifest not referenced by the installer and inconsistent with the shipped layout.</note>
            <note version="1.3.0" creationdate="2026-09-01">Fixed Joomla 3 deployability and consistency: added a Joomla-3 fileset to the file manifest so the template override (templates/tpl_jdseattle/html/mod_facilitycalendar_event_list/) and the scc-calendar-badge.png image actually deploy on install (previously neither was declared). Fixed the badge CSS path to resolve to the site /images/ directory from the media folder. Bumped the declared minimum PHP from 7.2 to 7.4 to match the typed-property code, in README and preflight.</note>
            <note version="1.2.0" creationdate="2026-09-01">Fixed Joomla 3.10 compatibility: removed the Joomla 4-only InstallerScriptInterface (which does not exist in Joomla 3.10 and caused a fatal "interface not found" at install); the installer script is now a duck-typed Joomla 3 InstallerScript. Bumped all version surfaces (package manifest, file manifest, update.xml, changelog) to 1.2.0 and corrected the update.xml download URL to the real release asset.</note>
            <note version="1.0.1" creationdate="2026-08-31">Production hardening: fixed installer postflight method (was using non-existent postinstall), replaced regex XML patching with DOMDocument, added LOCK_EX file locking, added is_writable() checks, added preflight compatibility checks, added uninstall rollback, externalized CSS to media file, fixed update server URL, fixed hardcoded image path, added language files, added index.html files, added blockChildUninstall, added changelogurl, bumped to Joomla 3.8+ installer interface.</note>
            <note version="1.0.0" creationdate="2026-08-19">Initial release. Card-style theme for mod_facilitycalendar_event_list with light rounded date tiles, hairline dividers, semibold titles, and muted times.</note>
        </notes>
    </extension>
</extensionset>
